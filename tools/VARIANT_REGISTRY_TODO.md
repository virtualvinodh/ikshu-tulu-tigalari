# variant_registry.json coverage report

Generated by `generate_variant_registry.py` - 57 single-glyph variant entries (32 bases), 80 ligature variant entries (78 bases).

## 2 alt glyph(s) with a base this registry doesn't handle

Base isn't consonant/vowel/vowel-sign/mark, and isn't classified under conjunct_ligature/vowel_sign_ligature/looped_virama_ligature either (i.e. it's not a real TutgAkhand ligature output) - e.g. vamatra-tutg, a drawing component used inside other glyphs, never itself a shaped output. Listed here for a future decision, not included in variant_registry.json.

- `vamatra-tutg.alt2`
- `vamatra-tutg.alt3`

## 3 alt glyph(s) with no real base glyph at all

The glyph name implies a base (`<name>-tutg`) that doesn't actually exist in the font - likely a typo or an internal tool artifact, not a real variant.

- `_smart.vamatra-tutg.alt1`
- `_uuMatra-tutg.alt2`
- `lVocalicMatra_tall-tutg.alt2`

